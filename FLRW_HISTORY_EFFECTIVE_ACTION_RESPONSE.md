# FLRW history effective-action response

Status: **algebraic/variational consumer closed; theory-specific BQG `Gamma_FLRW` remains OPEN_PHYSICAL.**

The dark-energy/background branch must be a restriction of the same physical effective action used for the perturbative sectors. This note freezes the deterministic map from a supplied homogeneous history effective action to its effective energy density, pressure and equation of state.

## 1. Convention

For one comoving coordinate-volume unit use

\[
ds^2=-N(t)^2dt^2+a(t)^2d\mathbf x^2
\]

and

\[
\Gamma_{hist}^{FLRW}=\int dt\,L_\Gamma(a,N,\dot a,\dot N).
\]

Freeze the covariant-metric variation convention

\[
\delta\Gamma_{hist}
=\frac12\int d^4x\sqrt{-g}\,T^{\mu\nu}_{hist}\,\delta g_{\mu\nu}.
\]

With fixed boundary variations this gives the exact minisuperspace relations

\[
\boxed{
\rho_{hist}=-\frac{1}{a^3}\,E_N[L_\Gamma]
}
\]

and

\[
\boxed{
p_{hist}=\frac{1}{3Na^2}\,E_a[L_\Gamma],
}
\]

where

\[
E_q[L]
=\frac{\partial L}{\partial q}
-\frac{d}{dt}\frac{\partial L}{\partial\dot q}.
\]

The implementation supports first-derivative minisuperspace Lagrangians and carries `addot`/`Nddot` generated by the Euler derivative symbolically.

When `rho_hist != 0`,

\[
\boxed{w_{hist}=\frac{p_{hist}}{\rho_{hist}}.}
\]

The separately conserved background identity is checked as

\[
\dot\rho_{hist}
+3\frac{\dot a}{a}(\rho_{hist}+p_{hist})=0.
\]

## 2. Exact controls

### Vacuum

For

\[
L_\Gamma=-Na^3\rho_0,
\]

one obtains

\[
\rho_{hist}=\rho_0,
\qquad
p_{hist}=-\rho_0,
\qquad
w_{hist}=-1.
\]

### Dust

For

\[
L_\Gamma=-N\rho_0,
\]

one obtains

\[
\rho_{hist}=\rho_0a^{-3},
\qquad p_{hist}=0,
\qquad w_{hist}=0.
\]

### Radiation

For

\[
L_\Gamma=-N\rho_0a^{-1},
\]

one obtains

\[
\rho_{hist}=\rho_0a^{-4},
\qquad p_{hist}=\rho_{hist}/3,
\qquad w_{hist}=1/3.
\]

A derivative-dependent control verifies that the code uses the full Euler derivative rather than only partial derivatives.

## 3. Physical fail-closed boundary

A symbolic expression can be interpreted as BQG background physics only when the packet certifies:

- `Gamma_FLRW` came from the theory-specific physical history;
- the absolute zero-source/background normalization was preserved rather than divided away independently at every geometry;
- the background metric variation convention was frozen;
- this is the same physical history/effective action used for the perturbative scalar/TT sectors;
- history measure and background convention hashes are supplied.

In particular,

```text
normalized local trace with W(0)=0
```

is **not** an absolute gravitational vacuum-energy prediction and may not be fed into this consumer as if its additive geometry-volume term were known.

## 4. Closed and open statements

Closed deterministic map:

\[
\boxed{
\Gamma_{FLRW}[a,N]
\longrightarrow
\rho_{hist},\ p_{hist},\ w_{hist}
}
\]

Open physical input:

\[
\boxed{
\text{theory-specific BQG }\Gamma_{FLRW}[a,N]
}
\]

including its absolute background/history normalization.

Therefore closing this consumer does not assert dark energy. A DE claim requires the actual BQG homogeneous history to yield a viable `rho_hist(a),p_hist(a)` without fitting a target `w(a)`.
