# Regulator-safe K5 HH reachable space

Status: **exact combinatorial size bound for the next quantum-HDA calculation**.

The Peter--Weyl exactness theorem says that an operator sequence is insensitive
to the spin cutoff provided no link reaches the cutoff wall. For the current
local Euclidean/Thiemann-type node Hamiltonian a single `H` can hit one link at
most twice, while an `HH` composition can hit a shared link at most four times.
Starting from the all-`j=1/2` K5 boundary therefore requires

$$
\boxed{J_{max}^{safe}=\frac52}.
$$

This bound is tight: the spin-label reachability enumeration actually contains
`j=5/2` histories after two local Hamiltonians.

## One-H reachable sector

From the all-`j=1/2` K5 boundary:

- raw spin assignments generated by one node Hamiltonian: `144`;
- Gauss-admissible assignments: **120**;
- total dimension after including four-valent intertwiner multiplicities:
  **816**.

## HH reachable sector

For every K5 vertex pair the same permutation-covariant count is obtained:

- raw spin assignments: `8411`;
- Gauss-admissible spin assignments: **4193**;
- total exact spin-network dimension: **24364**;
- largest spin actually reached: **j=5/2**.

Thus the correct exact problem is not a dense

$$
91^{10}
$$

link Hilbert space. It is a sparse Gauss/recoupling calculation on at most
24364 reachable spin-network states.

## Local-block compression

Across all five nodes and all 4193 admissible global spin assignments only

$$
\boxed{163}
$$

distinct local four-spin quartets occur. Their intertwiner-space dimensions are

- dimension 1: **112** distinct quartets;
- dimension 2: **44** distinct quartets;
- dimension 3: **7** distinct quartets.

The local multiplicity never exceeds three.

In a real recoupling basis the oriented four-valent volume generator is `i`
times a real antisymmetric matrix. Consequently:

- dimension 1: oriented volume vanishes;
- dimension 2: eigenvalues are `{-q,+q}`, hence absolute volume is scalar;
- dimension 3: eigenvalues are `{-q,0,+q}`, hence absolute volume distinguishes
  a zero-volume direction from a nonzero plane.

Therefore the genuinely non-scalar local volume content of the entire safe HH
reachable space is concentrated in only seven tiny `3 x 3` local blocks.

## Computational consequence

The next exact verifier should cache:

1. fundamental Clebsch--Gordan edge transitions;
2. the 163 local recoupling maps;
3. the seven nontrivial `3 x 3` absolute-volume blocks;
4. tetrahedral orientation signs.

It should then evaluate `H_k H_l - H_l H_k` directly in the reachable
spin-network basis. Magnetic-state brute force is unnecessary and is orders of
magnitude larger because it ignores the Gauss reduction.

The first explicit safe column using the genuine volume is reported separately
in `K5_PETER_WEYL_SAFE_HDA_FIRST_COLUMN.md`.
