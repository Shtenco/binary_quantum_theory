# Hamiltonian constraint to physical effective action

Status: **legal bridge and failure conditions fixed; theory-specific physical history remains open.**

This file is the missing bridge referenced by `FESHBACH_INTERBLOCK_EFFECTIVE_KERNEL.md`.  Its purpose is to prevent a constraint spectral kernel, finite projector theorem, local source susceptibility or clock-control frequency from being silently renamed a physical propagator.

The required chain is

\[
\boxed{
\{C_A\}
\to M_G
\to P_{phys}/\eta
\to Z_{phys}[J]
\to W_{phys}[J]
\to \Gamma[\phi]
\to \Gamma^{(2)}_{physical}
}
\]

with gauge/constraint reduction performed before physical poles or cosmological response functions are interpreted.

## 1. Regulated constraint family

At a fixed finite regulator let the graph-changing constraints be

\[
C_A.
\]

For a positive constraint-space metric `G`, define the master operator

\[
\boxed{
M_G=C_A^\dagger G^{AB}C_B\ge0.
}
\]

The exact finite theorem already registered in the repository gives

\[
\ker M_G=\bigcap_A\ker C_A.
\]

When the zero sector is isolated, a finite spectral projector can be defined.

This theorem is necessary but not sufficient for physical gravity.  The candidate still needs a controlled refinement/rigging/boundary-history limit and a physical inner product.

## 2. Constraint resolvent is not physical frequency response

For a coarse carrier `Q_0`, the Feshbach object

\[
G_c^{constraint}(z)
=Q_0^\dagger(z-H)^{-1}Q_0
\]

is an exact spectral resolvent of the chosen constraint operator.

Unless a physical time/history construction has independently established the identification,

\[
\boxed{z\ne\omega.}
\]

The same prohibition applies to finite C8 characters, Lanczos spectral coordinates and other internal labels.

They can be microscopic inputs to a physical history calculation; they are not physical poles by renaming.

## 3. Physical history / rigging amplitude

The target object is a theory-specific physical amplitude.  Schematically one may write

\[
\mathcal A_{phys}[J]
=
\langle B_f|
\eta\,\mathcal T
\exp\left(i\int J_I\,\mathcal O_I^{rel}\right)
|B_i\rangle,
\]

or the corresponding boundary/history sum generated by the same microscopic constraint system.

Here:

- `eta` denotes the physical rigging/projector/history operation supplied by the theory;
- `B_i,B_f` are physical boundary data if a boundary formulation is used;
- `O_I^rel` are relational or otherwise gauge-invariant source insertions;
- the measure and normalization must be derived or frozen before observables are read.

The finite C8 `R=J` construction proves that such ordering is mathematically possible.  It does not provide the gravitational `eta` or physical clock.

## 4. Connected generating functional

For response calculations define a normalized physical generating functional only after the physical amplitude exists,

\[
Z_{phys}[J]
=\frac{\mathcal A_{phys}[J]}{\mathcal A_{phys}[0]},
\]

when that normalization is appropriate for the observable in question, and

\[
W_{phys}[J]
=-i\hbar\log Z_{phys}[J]
\]

in Lorentzian convention.

Connected source derivatives come from `W`, not from a raw extensive constraint moment.

For Euclidean/finite positive controls the repository uses the corresponding real logarithm and ordinary convex Legendre transform.  Sign and `i` conventions must not be mixed across the two settings.

## 5. 1PI Legendre transform

Let

\[
\phi_I=\frac{\delta W}{\delta J_I}.
\]

In a real convex convention,

\[
\Gamma[\phi]
=J_I\phi_I-W[J],
\]

and on a nonsingular physical source quotient

\[
\boxed{
\Gamma^{(2)}=(W^{(2)})^{-1}.
}
\]

In Lorentzian convention the corresponding factors of `i` and signs must be carried consistently; physical poles are read only from the final causal kernel.

The inverse is **not** taken on unreduced gauge directions.

## 6. Why the first q=2 source is not yet cosmological Gamma_scalar

The exact registered source

\[
Z(j_X,j_Z)=\cosh\sqrt{j_X^2+j_Z^2}
\]

has an exact local Legendre transform

\[
\Gamma_{shape}(s)
=s\operatorname{artanh}s
+\frac12\log(1-s^2).
\]

That is a genuine exact finite 1PI positive-control object.

However its metric carrier is only the two-dimensional trace-free `X/Z` shape tangent.  It lacks:

- the common-flux-scale / spatial conformal mode;
- a physical lapse-response source;
- longitudinal shift/diffeomorphism constraint reduction;
- connected interblock history;
- a physical time/frequency construction;
- a conserved matter/probe response interface.

Therefore it is not `Gamma_scalar(omega,k)`.

## 7. Scalar ADM carrier and gauge reduction

Before a cosmological scalar kernel is interpreted, a local ADM scalar decomposition contains, schematically,

\[
\chi_{scalar}
=
(\delta N,\ B,\ \zeta,\ E,\ldots),
\]

where

- `delta N` is the lapse perturbation;
- `N_i=\partial_i B` is the longitudinal shift;
- `zeta` is the spatial conformal/volume perturbation;
- `E` is scalar shear;
- additional collective/matter variables may appear if the microscopic theory genuinely supplies them.

In GR, lapse and shift implement constraints and the pure-gravity scalar sector does not contain an ordinary propagating scalar graviton.  Therefore a BQG calculation must not count an unreduced lapse, shift or conformal gauge direction as dark matter.

There are two legal routes:

1. derive a gauge-invariant/relational scalar observable basis before constructing the final Hessian; or
2. construct the full source/constraint block and eliminate gauge/constraint variables by the correct constrained Schur/Dirac reduction.

A schematic quadratic block form is

\[
K=
\begin{pmatrix}
K_{cc}&K_{cp}\\
K_{pc}&K_{pp}
\end{pmatrix},
\]

with `c` denoting lapse/shift constraint variables and `p` the retained physical/response variables.  Where an ordinary Schur reduction is legal,

\[
K_{eff}=K_{pp}-K_{pc}K_{cc}^{-1}K_{cp}.
\]

If `K_cc` is singular because a variable is a true Lagrange multiplier, the constraint must be imposed rather than numerically inverted.  Gauge fixing or quotienting must preserve the Ward/HDA identities.

## 8. Conformal static potential versus Lorentzian kinetic signature

The new collective `j=1` normalized volume source gives a convex static Legendre potential with

\[
\Gamma_V''(p_0)=\frac92>0.
\]

The flux-space DeWitt theorem gives the common conformal kinetic direction

\[
Q_{DW}=-6.
\]

These signs do not contradict each other.

`Gamma_V''` is a **static source susceptibility/effective-potential curvature** in a normalized finite kinematic ensemble.

`Q_DW=-6` is the signature of the **Lorentzian canonical kinetic supermetric** before full constraint reduction.

A physical scalar theory needs the frequency-dependent history kernel

\[
\Gamma^{(2)}_{scalar}(\omega,k),
\]

not a sign identification between these two unrelated Hessians.

This distinction is a frozen claim-discipline rule: neither sign may be used alone to declare stability, a ghost, dark matter or dark energy.

## 9. Connected interblock kernel and momentum

For independent projected blocks,

\[
Z=\prod_b Z_b,
\qquad
W=\sum_bW_b,
\]

so every connected cross-block Hessian is zero.

A spatial momentum kernel appears only when the theory-specific physical history produces nonfactorizing block correlations.

The tetrahedral nearest-neighbor geometry already fixes the leading moment

\[
\sum_{a=1}^4 n_an_a^T=\frac43I,
\]

so an `S4`-symmetric scalar nearest-neighbor history transfer has an isotropic leading `k^2` structure.  But its coefficient is a microscopic physical-history output, not a fit parameter.

Constraint Feshbach/nearest-block data may constrain this coefficient, but cannot be promoted directly to physical `omega,k` response.

## 10. Conserved matter/probe response

A vacuum metric kernel is insufficient to define `mu_BQG` and `Sigma_BQG`.

The physical variational problem must contain one universal conserved source coupling, schematically

\[
S_{probe}^{(1)}
=\frac12\int\sqrt{-\bar g}\,h_{\mu\nu}T^{\mu\nu},
\qquad
\bar\nabla_\mu T^{\mu\nu}=0.
\]

Then the scalar response can produce `Phi/Psi` and the dynamics/lensing observables from the same kernel and the same coupling normalization.

See `BQG_SCALAR_RESPONSE_TO_MATTER.md`.

## 11. From physical scalar kernel to mu and Sigma

Only after the source coupling, gauge reduction and physical kernel are frozen may one define, in a stated cosmological convention,

\[
-k^2\Psi
=4\pi G_{ref}a^2\mu_{BQG}(a,k)\rho\Delta,
\]

\[
-k^2(\Phi+\Psi)
=8\pi G_{ref}a^2\Sigma_{BQG}(a,k)\rho\Delta.
\]

The same response must determine dynamics, weak/strong/CMB lensing and Fermat/time-delay phase.  Independent post-hoc `mu` and `Sigma` fits are forbidden.

## 12. Homogeneous background and dark energy

The FLRW sector is a separate projection of the **same** physical effective action.

One must evaluate the homogeneous history/effective action, vary it with respect to the background metric/scale variables and derive

\[
\rho_{hist}(a),\qquad p_{hist}(a).
\]

Only afterward may one infer

\[
w_{hist}(a)
=-1-\frac13\frac{d\ln\rho_{hist}}{d\ln a}
\]

when the effective component is separately conserved in the chosen description.

A normalized local finite trace with `W(0)=0` cannot fix the absolute gravitational vacuum term: an additive metric-volume term is physically relevant in gravity.  Therefore zero-source normalization is not a prediction of the cosmological constant.

## 13. Failure conditions

The bridge fails if any of the following shortcuts is used:

- rename constraint spectral `z` as physical `omega`;
- invert an unreduced gauge Hessian and call gauge poles particles;
- identify lapse/shift smearing labels with physical Bardeen potentials;
- read a local `X/Z` source Hessian as a long-range Poisson kernel;
- treat the kinematic `j=1` normalized trace as `Gamma_FLRW`;
- infer vacuum energy from `W(0)=0` after normalization;
- define `mu/Sigma` without one conserved source coupling;
- use different effective potentials or source normalizations for dynamics and lensing;
- fit the missing interblock transfer coefficient to the target observation and call it microscopic output.

## 14. Current status

The repository currently has exact or finite controls for many individual arrows, but the theory-specific chain

```text
actual graph-changing constraints
-> refinement-compatible physical history
-> source-dressed connected multi-block W[J]
-> gauge-reduced Gamma_scalar^(2)(omega,k)
-> conserved source response
-> Phi/Psi
-> mu_BQG/Sigma_BQG
```

remains open.

This file defines the interface that the next computation must satisfy.  It does not close the physicalization gates by documentation alone.
