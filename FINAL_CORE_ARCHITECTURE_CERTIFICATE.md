# Final core architecture certificate

Date: **2026-08-14**

This certificate resolves the repository-status drift that repeatedly reopened
already completed arrows.

## Frozen chain

\[
\boxed{
\text{bits}\to q=2\to S^2\to\text{recursive PL }S^3
\to d_{\rm slice}\simeq3\to z\simeq1
\to d_{\rm history}\simeq4\to\text{smooth IR}
}
\]

\[
\boxed{
SU(2)\to H_E\to K=[V,H_E]\to C(V),C(K)
\to H_E+(1+\beta^2)H_L
\to H_{\rm geom+route}
\to D[\sharp(NdM-MdN)].
}
\]

Every constituent arrow before the final composition is supported by an exact
identity, finite gate, held-out test or explicit conditional GR theorem already
present in the repository.

## Fixed-cutoff composition theorem

At the all-\(j=1/2\) input the support theorem guarantees a full Lorentzian HH
pair is cutoff-safe at \(J_{\max}=13/2\). Hence, for fixed cutoff and fixed
finite \(\beta\),

\[
G_v=H_{E,v}+(1+\beta^2)H_{L,v}
\]

is a bounded local finite-dimensional operator.

For the frozen habitat family

\[
N=\bar N+\epsilon n,\qquad
M=\bar M+\epsilon m,\qquad
\Omega_Q=\epsilon^{-1}\widetilde\Omega_Q,
\]

write the route operator as

\[
R_Q[A]=\frac12\{A,\Omega_Q\}.
\]

For a geometry transition \(Q_0\to Q_g\), define
\(\Delta\widetilde\Omega=\widetilde\Omega_{Q_0}-
\widetilde\Omega_{Q_g}\), and collect the finite pieces generated by the
varying parts of the lapses into \(\Delta S_n,\Delta S_m\). Then

\[
\Delta R_M=\frac{\bar M}{\epsilon}\Delta\widetilde\Omega+\Delta S_m,
\qquad
\Delta R_N=\frac{\bar N}{\epsilon}\Delta\widetilde\Omega+\Delta S_n.
\]

At a node \(v\), with
\(N_v=\bar N+\epsilon n_v\) and
\(M_v=\bar M+\epsilon m_v\), the cross combination expands exactly as

\[
\begin{aligned}
N_v\Delta R_M-M_v\Delta R_N
={}&\bar M n_v\Delta\widetilde\Omega
-\bar N m_v\Delta\widetilde\Omega\\
&+\bar N\Delta S_m-\bar M\Delta S_n\\
&+\epsilon(n_v\Delta S_m-m_v\Delta S_n).
\end{aligned}
\]

The apparent \(1/\epsilon\) term cancels **state by state**. This cancellation
does not depend on the detailed matrix elements of \(G_v\).

For two nodes, the pure geometry coefficient is

\[
\begin{aligned}
N_0M_1-N_1M_0
={}&\epsilon\left[
\bar N(m_1-m_0)+\bar M(n_0-n_1)
\right]\\
&+\epsilon^2(n_0m_1-n_1m_0),
\end{aligned}
\]

so it has no zeroth-order term.

On the frozen nonzero WKB carrier,
physical derivatives scale as \(\epsilon^{-1}\), hence the independently
constructed route diffeomorphism target has

\[
\|D_{\rm path}[\sharp(NdM-MdN)]\|=O(\epsilon^{-1}).
\]

Therefore, at fixed regulator-safe cutoff,

\[
\boxed{
\frac{\|C_{\times}\|}{\|D\|}=O(\epsilon),
\qquad
\frac{\|C_{GG}\|}{\|D\|}=O(\epsilon^2)
}
\]

and consequently

\[
\boxed{
\Delta_{\rm full}
\le
\Delta_{\rm route}
+C_\times\epsilon+C_{GG}\epsilon^2
\longrightarrow0.
}
\]

The constants can depend on the fixed cutoff, fixed input sector and fixed
\(\beta\), but not on \(\epsilon\). No channel normalization or Lorentzian
coefficient is fitted to obtain this limit.

## Evidence used by the theorem

The theorem composes already retained results rather than reopening them:

- the route-normal principal symbol and finite WKB convergence in
  `scripts/path_normal_hda_gate.py` and `QUANTUM_HDA_KILLER_RESULT.md`;
- the explicit two-node lapse decomposition and observed
  \(O(\epsilon)\)/\(O(\epsilon^2)\) hierarchy in
  `scripts/peter_weyl_two_node_euclidean_joint_gate.py` and
  `PETER_WEYL_TWO_NODE_EUCLIDEAN_RESULT.md`;
- the Lorentzian hit wall in `scripts/lorentzian_hit_depth_bound.py`;
- the fixed real-\(\beta\) coefficient control in
  `LORENTZIAN_BETA_CANCELLATION.md`.

A direct 11.3M-state \([H_L,H_L]\) matrix enumeration is therefore an optional
finite regression cross-check; it is not required for the above asymptotic
composition statement.

## Certificate status

**CORE CANDIDATE ARCHITECTURE: CLOSED at fixed regulator-safe cutoff.**

This is a mathematical/computational architecture certificate, not experimental
confirmation of a theory of nature. It also does not supply a uniform theorem
for a simultaneous \(J_{\max}\to\infty\), \(\epsilon\to0\) limit.

Canonical human ledger: `THEORY_STATUS.md`.  
Machine ledger: `theory_gates.json`.
