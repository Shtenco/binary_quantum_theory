# Deprecated compatibility path

This historical filename is retained only so old links do not break.

The canonical result is now:

`FIXED_CUTOFF_COMPOSITION_BOUND.md`

There is **no final architecture certificate and no claim that full quantum HDA is closed**.

The retained mathematical statement is only a fixed-cutoff composition estimate: on the explicitly declared finite-dimensional habitat and regulator-safe cutoff, the route-normal target carries the leading derivative scaling while the added cross and pure-geometry terms are suppressed relatively as

$$
\frac{\|C_{cross}\|}{\|D\|}=O(\epsilon),
\qquad
\frac{\|C_{GG}\|}{\|D\|}=O(\epsilon^2).
$$

See `FIXED_CUTOFF_COMPOSITION_BOUND.md` for the assumptions, derivation, evidence and exact claim boundary.

The following remain open and are not certified by this result:

```text
full graph-changing multi-node off-shell quantum HDA closure
uniform simultaneous cutoff/refinement removal
microscopic derivation of the required geometric quantum sector
physical scale setting
blind external experimental prediction
```

Canonical status: `THEORY_STATUS.md`.  
Machine ledger: `theory_gates.json`.
