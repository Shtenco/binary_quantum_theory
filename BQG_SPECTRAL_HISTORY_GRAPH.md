# BQG spectral-history graph from the master constraint

Status: **exact finite-regulator theorem + actual Euclidean first-edge implementation; physical Lorentzian/refinement history remains fail-closed.**

This module takes the useful part of spectral-graph reasoning — spectral measures, heat kernels, gaps, zeta functions and Krylov/Jacobi reductions — but derives the graph from the BQG constraint operator itself. No small-world topology, coordination number, channel weight or target physical constant is fitted to manufacture the spectrum.

## 1. Object being reduced

For a finite regulated constraint family `C_A` and any positive constraint-label metric `G`,

\[
\mathbb M_G=C_A^\dagger G^{AB}C_B\ge 0,
\qquad
\ker \mathbb M_G=\bigcap_A\ker C_A.
\]

When zero is isolated,

\[
P_{phys}^{(\epsilon)}=\mathbf 1_{\{0\}}(\mathbb M_G),
\qquad
\|e^{-T\mathbb M_G}-P_{phys}\|=e^{-T\Delta_M}.
\]

The production gate uses the repository's Hermitian Euclidean ordering

\[
H^{sine}_{E,v}=\frac{T_v-T_v^\dagger}{2i}
\]

and the positive finite K5 Euclidean master

\[
\boxed{\mathbb M_E=\sum_{v\in K5}(H^{sine}_{E,v})^\dagger H^{sine}_{E,v}
=\sum_{v\in K5}(H^{sine}_{E,v})^2.}
\]

This is an operator-derived finite Euclidean constraint object. It is not the full Lorentzian/global physical BQG history.

## 2. Derived block-Jacobi spectral graph

For an orthonormal seed block `Q_0`, define

\[
\mathcal K_r(\mathbb M,Q_0)=\operatorname{span}\{Q_0,\mathbb M Q_0,\ldots,\mathbb M^rQ_0\}.
\]

Variable-rank block Lanczos constructs

\[
A_n=Q_n^\dagger\mathbb M Q_n
\]

and

\[
\boxed{\mathbb M Q_n=Q_{n-1}B_n^\dagger+Q_nA_n+Q_{n+1}B_{n+1}.}
\]

With

\[
R_{n+1}=\mathbb M Q_n-Q_{n-1}B_n^\dagger-Q_nA_n,
\]

write

\[
R_{n+1}^\dagger R_{n+1}=U\Lambda U^\dagger.
\]

For the resolved positive-rank part,

\[
Q_{n+1}=R_{n+1}U_r\Lambda_r^{-1/2},
\qquad
B_{n+1}=\Lambda_r^{1/2}U_r^\dagger.
\]

The resulting graph is not chosen phenomenologically:

\[
J_r=
\begin{pmatrix}
A_0&B_1^\dagger&0&\cdots\\
B_1&A_1&B_2^\dagger&\cdots\\
0&B_2&A_2&\cdots\\
\vdots&\vdots&\vdots&\ddots
\end{pmatrix}.
\]

Its block ranks, vertices and edge weights are generated by `M` and `Q_0`.

## 3. Exact finite closure criterion

In exact arithmetic the finite seed history closes at depth `r` iff

\[
\boxed{B_{r+1}=0}
\]

or equivalently `R_{r+1}=0`. Then the cyclic Krylov space is invariant.

If `Q=(Q_0,...,Q_r)` and `E_0` injects the seed into the first Jacobi block, closure gives

\[
\mathbb M Q=QJ_r
\]

and therefore, for every spectral function `f` defined on the finite spectrum,

\[
\boxed{Q_0^\dagger f(\mathbb M)Q_0=E_0^\dagger f(J_r)E_0.}
\]

In particular,

\[
Q_0^\dagger e^{-\sigma\mathbb M}Q_0=E_0^\dagger e^{-\sigma J_r}E_0,
\]

\[
Q_0^\dagger\mathbf 1_{\{0\}}(\mathbb M)Q_0
=E_0^\dagger\mathbf 1_{\{0\}}(J_r)E_0.
\]

A certified closed graph therefore contains the entire **finite-regulator constraint spectral history seen by the chosen seed sector**, including its projected zero-sector projector. It does not merely fit finitely many moments.

## 4. First edge from actual moments

For orthonormal `Q_0`,

\[
A_0=Q_0^\dagger\mathbb M Q_0=\mu_1,
\]

\[
\mu_2=(\mathbb M Q_0)^\dagger(\mathbb M Q_0)=Q_0^\dagger\mathbb M^2Q_0.
\]

Hence

\[
\boxed{R_1^\dagger R_1=\mu_2-A_0^\dagger A_0=B_1^\dagger B_1.}
\]

`scripts/bqg_spectral_history_graph_gate.py --mode peter-weyl-euclidean-first-edge` evaluates this identity using the actual repository Peter-Weyl Euclidean operator and the complete 32-state all-`j=1/2` logical K5 seed sector.

If `rank(B_1)>0`, closure has **not** happened. The next direction must be computed rather than discarded.

## 5. Matrix-valued spectral measure after closure

For

\[
J_ru_a=\lambda_au_a,
\]

define

\[
W_a=E_0^\dagger u_au_a^\dagger E_0,
\qquad
d\Sigma_{Q_0}(\lambda)=\sum_aW_a\delta(\lambda-\lambda_a)d\lambda.
\]

Then

\[
\mu_n=\int\lambda^n d\Sigma_{Q_0}(\lambda).
\]

For a finite positive spectrum,

\[
\zeta_{Q_0}(s)=\sum_{\lambda_a>0}\operatorname{tr}(W_a)\lambda_a^{-s},
\]

and

\[
H_{Q_0}(\sigma)=\sum_a e^{-\sigma\lambda_a}\operatorname{tr}W_a.
\]

The diagnostic

\[
\boxed{d_{s,M}^{(Q_0)}(\sigma)=2\sigma
\frac{\sum_a\lambda_ae^{-\sigma\lambda_a}\operatorname{tr}W_a}
{\sum_ae^{-\sigma\lambda_a}\operatorname{tr}W_a}}
\]

is called **constraint spectral dimension**. It is not physical spacetime dimension unless a separate refinement/geometry theorem establishes that relation.

## 6. What is retained from phenomenological spectral-graph work

Useful transferable machinery:

- spectra and spectral measures;
- heat kernels and spectral gaps;
- zeta functions when mathematically defined;
- multiscale/refinement comparison;
- spectral dimension as a diagnostic;
- operator-derived graph reductions.

Not imported:

- choosing spatial dimension first and then treating it as an output;
- choosing a coordination number because it matches a desired dimension;
- fitting small-world probabilities/channel weights to physical constants;
- replacing divergent lattice sums by one-dimensional zeta values without a declared regularization;
- changing a `zeta(4)` contribution to `zeta(2)` without a derivation;
- using numerical agreement with a target as proof of an independent prediction.

The BQG rule is:

> **the operator chooses the graph; the graph does not choose the answer.**

## 7. Claim boundary

### Closed algebraically

The deterministic regression verifies variable-rank block-Lanczos closure, exact compression on the invariant cyclic space, heat-kernel equality, zero-projector equality, spectral-weight sum rule and positivity to floating-point tolerance.

### Actual BQG finite Euclidean calculation

The production first-edge gate computes `A0`, `mu2`, `R1^dag R1`, `B1` rank and the first recurrence using the actual Peter-Weyl `H_sine` implementation. Its JSON evidence decides whether depth-zero closure happened.

### Still physically open even after finite spectral closure

Finite Euclidean closure does **not** by itself provide:

- the full Lorentzian/global regulated constraint family;
- anomaly-controlled regulator removal;
- the continuum rigging map / physical inner product;
- a derived relational clock or equivalent boundary-history prescription;
- connected source-dressed `W[J]`;
- gauge-reduced `Gamma[g]`;
- physical `K_TT(omega,k)` or scalar response;
- dark matter, dark energy or experimental predictions.

Those arrows remain governed by `MASTER_CONSTRAINT_PHYSICAL_PROJECTOR.md`, `HAMILTONIAN_CONSTRAINT_TO_EFFECTIVE_ACTION.md` and the fail-closed physicalization ledgers.

## 8. Next exact calculation

If the first edge returns `B1_rank > 0`, the preregistered next object is

\[
A_1=Q_1^\dagger\mathbb M_EQ_1,
\]

followed by

\[
R_2=\mathbb M_EQ_1-Q_0B_1^\dagger-Q_1A_1.
\]

Only an actually resolved `rank(R_2^dag R_2)=0` may close at depth 1; otherwise the Lanczos chain continues. This is a finite falsifiable stopping rule rather than an arbitrary truncation depth.
