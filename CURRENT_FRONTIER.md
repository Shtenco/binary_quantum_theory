# Current frontier — post-HDA physicalization

**Canonical priority reset: 2026-08-18.**

This file exists to prevent the research programme from repeatedly reopening already-tested HDA work instead of advancing the physical prediction chain.

## Hard routing rule

**Do not treat another HDA variant, another node-count extension, or another K5 covariance regression as the next milestone.**

The repository already contains the structural HDA/ADM prerequisite stack: route/principal-symbol controls, Peter–Weyl finite graph-changing calculations, Lorentzian/fixed-cutoff composition controls and a three-node graph-changing finite regression. The strongest arbitrary-graph Lorentzian theorem may remain formally open in the global ledger, but it is **deferred**, not the current physical bottleneck.

For the current programme, HDA is a frozen dependency.

## Current no-loop chain

```text
existing regulated constraints C_A
 -> positive master constraint M = C^dag G C
 -> finite physical zero/low-energy projector P_phys
 -> regulator/refinement-compatible rigging/history limit
 -> source-deformed physical amplitude Z[J_g]
 -> connected W[J_g] = -i log Z[J_g]
 -> Gamma[g]
 -> physical TT Hessian K_TT(omega,k)
 -> six on-shell quartic Wilson coefficients c1..c6
 -> one common scale rule
 -> preregistered blind external observable
 -> comparison with held-out experiment
```

No arrow above is replaced by another HDA calculation.

## What is already available and should be reused

1. Microscopic q=2 geometry carrier and Peter–Weyl representation bridge.
2. Regulated Euclidean/Lorentzian constraint operators and finite support controls.
3. HDA/ADM structural consistency results.
4. Regge/Plebanski/metric reconstruction controls.
5. The exact finite master-constraint theorem:

   `M_G = sum_AB C_A^dag G^{AB} C_B >= 0`, with positive-definite `G`, and

   `ker M_G = intersection_A ker C_A`.

6. The complete six-dimensional parity-even tetrahedral/S4 quartic TT observable basis from the earlier physicalization branch.

## Immediate computation

The next gravity-specific computation is **not** an abstract projector toy and not an HDA test. It is to feed the repository's actual regulated Peter–Weyl constraints into a master-constraint/projector calculation on a declared finite carrier/habitat, report its spectrum and physical-projector diagnostics, and then compare those matrix elements across a refinement sequence.

A successful finite stage must report at minimum:

- declared domain/carrier and all included constraints;
- master spectrum and numerical positivity residual;
- exact or tolerance-defined zero-sector rank;
- first positive master gap when a zero sector exists;
- dependence/independence of the zero projector under several positive constraint metrics `G`;
- heat-kernel convergence diagnostics;
- metric-observable matrix elements inside the selected physical sector;
- explicit statement that no spectral variable has been renamed physical `omega`.

If the exact finite zero sector is empty, that is a scientific result: the calculation must report it and move to a controlled spectral-window/rigging analysis rather than retuning HDA.

## Physical frequency rule

The spectral parameter of a constraint resolvent is **not** physical frequency by notation. Physical `omega` enters only after a physical history/projector construction or a derived relational clock produces the connected metric effective action.

The legal chain is

`constraints -> physical projector/history -> Z[J] -> W[J] -> Gamma[g] -> K_TT(omega,k)`.

## Prediction rule

Do not use `d≈3`, `z≈1`, the S4 oracle `Lambda=3`, or any quantity used in model selection as a blind prediction.

Freeze a blind external prediction only after:

1. the physical TT kernel exists;
2. the six dimensionless pole coefficients are frozen;
3. one common physical scale rule is fixed;
4. estimator, uncertainty prescription and held-out dataset identifier are committed before target inspection.

## Current priority labels

```text
HDA_STRUCTURAL_PREREQUISITE   = FROZEN / REUSE
ARBITRARY_GRAPH_HDA_THEOREM   = DEFERRED OPEN, NOT CURRENT BOTTLENECK
MASTER_PROJECTOR_THEOREM      = EXACT FINITE MATHEMATICS
PHYSICAL_PROJECTOR_HISTORY    = CURRENT FRONTIER
CONNECTED_INTERBLOCK_HISTORY  = NEXT
PHYSICAL_TT_KERNEL            = NEXT
IR_SIX_VECTOR                 = NEXT
BLIND_EXTERNAL_PREDICTION     = FINAL FRONTIER
```
