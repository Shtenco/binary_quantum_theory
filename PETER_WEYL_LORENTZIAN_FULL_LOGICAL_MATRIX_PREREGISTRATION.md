# Preregistration: full Lorentzian logical matrix and symmetry audit

Status: **frozen before the first 24-term epsilon logical-return result is known**.

If the first-column witness is computationally valid, the next direct-return object is the complete matrix

\[
R_v=P_{log}H_{L,v}^{raw}P_{log}
\]

on the canonical 32-dimensional all-`j=1/2` K5 logical basis.

This matrix is an operator-structure diagnostic. It is **not** the physical master constraint; complete outgoing-state Gram matrices remain mandatory as described in `PHYSICAL_LOGICAL_MASTER_GRAM_PREREGISTRATION.md`.

---

## 1. Frozen column protocol

Every one of the 32 columns uses exactly the same:

- sine-Hermitian `H_E^sine=(T-T^dagger)/(2i)`;
- `K=[V,H_E^sine]`;
- `C_e(O)=h_e[h_e^-1,O]`;
- 24-term four-face epsilon assembly;
- `Jmax=7/2` single-`H_L` support wall;
- scalar/covariant leakage thresholds;
- zero-aware volume convention;
- canonical Gauss/logical return map.

No column-specific threshold, phase, normalization, or basis rotation may be chosen after seeing its amplitude.

---

## 2. Raw matrix first, convention second

The raw 32x32 complex matrix `R_v` must be recorded before selecting a Hermitian physical-constraint convention.

Report separately

\[
R_H=\frac{R_v+R_v^\dagger}{2},
\]

and

\[
R_A=\frac{R_v-R_v^\dagger}{2i}.
\]

Record

\[
\frac{\|R_v-R_v^\dagger\|_F}{\max(\|R_v\|_F,\epsilon)}
\]

and

\[
\frac{\|R_v+R_v^\dagger\|_F}{\max(\|R_v\|_F,\epsilon)}.
\]

Neither `R_H` nor `R_A` is selected merely because one has a larger norm. Overall factors of `i`, `hbar`, `beta` and the final Hermitian Lorentzian ordering must be fixed from the declared constraint convention / HDA bridge rather than from a desired spectrum.

---

## 3. Five-logical-qubit Pauli audit

The all-`j=1/2` K5 carrier is a tensor product of five two-dimensional logical intertwiner spaces. Once the canonical basis ordering is verified, decompose

\[
R_v=\sum_{a_0\ldots a_4}
 c_{a_0\ldots a_4}
 \sigma_{a_0}\otimes\cdots\otimes\sigma_{a_4},
\]

with

\[
a_i\in\{I,X,Y,Z\}.
\]

Report all coefficients above a fixed relative numerical threshold and the total Frobenius weight grouped by Pauli support size and number of `Y` factors.

No coefficient is interpreted as a mass or coupling before physical projection.

---

## 4. Source-stabilizer S4 sign-character test

Existing exact representation theory gives, for one logical geometry qubit `E=[2,2]`,

\[
\operatorname{End}(E)=A_1(I)\oplus A_2(Y)\oplus E(X,Z).
\]

The `S4` sign-character operator sector is exactly one-dimensional at one cell and generated by

\[
\boxed{Y}.
\]

For two cells under a diagonal `S4` action, the exact sign sector is

\[
\boxed{\operatorname{span}\{I\otimes Y,\;Y\otimes I,\;X\otimes Z-Z\otimes X\}.}
\]

The full epsilon-oriented `R_v` must therefore be tested against the exact source-node stabilizer action. Two hypotheses are recorded **before amplitudes are known**:

1. if `R_v` transforms with the sign character under the declared orientation-sensitive `S4` relabeling, its one-/two-cell components must lie in the exact sign sectors above;
2. if the complete physical orientation convention instead makes `R_v` an ordinary scalar, the sign projection must vanish and the ordinary scalar irreps must be used.

The transformation character is measured from the matrix; it is not chosen post hoc.

---

## 5. Symmetry-based column reconstruction

Computational acceleration by source-stabilizer or K5 vertex relabeling is permitted only if an explicit unitary representation `U(g)` on the 32D logical basis is constructed and the covariance law

\[
U(g)R_vU(g)^\dagger
=\chi(g)R_{g(v)}
\]

with the measured character `chi(g)` is verified.

At least one nontrivial column not used in the reconstruction must be computed directly as a held-out symmetry check.

Until that check passes, symmetry may classify the matrix but may not replace missing amplitude columns.

---

## 6. Relation to the physical master

Even a complete exact `R_v` does not determine the physical zero sector because

\[
P_{log}H_EP_{log}=0
\]

while the complete Euclidean outgoing Gram is positive definite on the same 32D carrier.

The production master therefore remains based on complete images

\[
\langle H_{E/L,v}\psi_i|H_{E/L,w}\psi_j\rangle
\]

in the enlarged Peter--Weyl habitat, not merely on products of direct-return matrices.

---

## Claim boundary

This preregistration freezes the matrix/symmetry diagnostics only. A nonzero orientation `Y` channel would establish a finite logical Lorentzian return, not a cosmological scalar, matter field, physical pole, dark matter, dark energy or a measured parity violation.