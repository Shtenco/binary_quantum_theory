# Physical closure ledger v3

Date: 2026-08-17

Status: **candidate quantum-gravity / emergent-gauge programme; not experimentally established.**

This ledger separates four logically different kinds of statement so that no finite calculation or conditional infrared theorem is silently promoted into a microscopic prediction.

---

## A. KINEMATIC / EXACTLY PROVED IN THE DECLARED CONSTRUCTION

### Geometry

- `q+2=2^q` selects the unique positive integer `q=2`.
- The q=2 local route shell is the octahedral `S2` link.
- The frozen minimal+flag global completion is the 16-cell boundary in the PL class of `S3`.
- The recursive q=2 counting law is
  \[
  N_g=(4\,8^g+10)/7,
  \]
  and its finite effective dimension tends monotonically to three.
- The selected `S3` spatial slice is spin and has a unique spin structure because `H^1(S3,Z2)=0`.

### q=2 projective structure

- Normalized q=2 states form `S3 subset C2`; rays form `CP1 ~= S2`.
- The Hopf fiber gives a compact `U(1)` phase connection.
- Its first Chern number is one in the frozen normalization.
- The quantum geometric tensor unifies Fubini-Study distinguishability geometry and Berry curvature as its real and imaginary geometric parts.

### Metric carrier

- The logical shape doublet has an exact rank-two trace-free shape-to-metric Jacobian.
- The five trace-free metric modes decompose under tetrahedral symmetry as `E + T2`.
- Generic directed-momentum parity-even quartic TT response has exactly six independent `S4` structures.
- The frozen six-observable extractor is invertible with
  \[
  \det A=1/699840000.
  \]
- The exact rotationally invariant Wilson vector is
  \[
  v_{iso}=(6,24,6,36,-9,18)^T,
  \]
  and `A v_iso = (1,1,1,1,1,1)^T`.

### Constraint/projector mathematics

- For any finite constraint family and any positive constraint metric `G`,
  \[
  M_G=C_A^\dagger G^{AB}C_B\ge0,
  \qquad
  \ker M_G=\bigcap_A\ker C_A.
  \]
- An isolated zero sector defines a unique orthogonal finite physical projector.
- Heat-kernel convergence is controlled by the first positive master gap.
- A solvable constrained two-level model verifies exactly that relational clock conditioning of the timeless projector reconstructs ordinary unitary evolution.

### Compact U(1) canonical structure

Conditional only on the existence of a positive local quadratic phase action, gauge covariance gives the exact canonical identities

\[
p=Z_AM_1(\dot\theta-d_0A_0),
\qquad
d_0^Tp=0,
\]

and

\[
\ddot\theta=-M_1^{-1}d_1^TM_2d_1\theta.
\]

The overall `Z_A` cancels from the linear wave operator.

---

## B. COMPLETED FINITE DYNAMICAL / NUMERICAL CERTIFICATES

- q=2 PL topology and recursive refinement gates: pass in declared finite windows.
- Structural scaling: `d_H ~= 3`, `z ~= 1`, slice/history spectral dimensions near `3/4` in the frozen numerical analyses.
- HDA/ADM tensor-structure gate selects `c=1/2`, `AB=1` in its declared family.
- Reduced TT positive control is massless with equal-time `P_TT(k) ~ k^-1`.
- Bare lattice quartic numbers `eta2=-1/45`, `zeta4=-1/12` are valid regulator coefficients only.
- First q4 refined metric tangent kernel has finite local precursor
  \[
  \Delta_{ET}/\kappa_5=0.08430036026012608.
  \]
- Exact j=1/2 higher-shell Peter-Weyl `Lambda` is positive/non-scalar and closes its block-Lanczos identities.
- The j=1 S4 coarse geometry doublet is a finite pass with machine-precision intertwiner/isometry/volume tests.
- The finite master-projector selftest passes in CI for four unrelated positive constraint metrics, with common zero-projector agreement at the `10^-15` level.
- Held-out Regge `L=6` TT residue prediction passed at about `0.00714%` relative error.
- Directional Regge residues `(100),(110),(111)` all trend toward the frozen `1/8` leading normalization; finite diagonal directions remain less asymptotic than axial.
- q=2/U1 seed cochain positive control has `b1=0`, gauge zero modes only, and unit-Hodge curl spectrum `0^7,4^6,6^8,8^3`.

None of these finite statements by itself supplies the physical interacting graviton six-vector, the photon stiffness `Z_A`, Standard-Model masses, or experimental validation.

---

## C. CONDITIONAL IR THEOREMS

These are powerful **if their assumptions are dynamically realized**.

### Gravity/light-cone universality

If the IR vacuum is ordinary Lorentz invariant, diffeomorphism symmetry is unbroken, the only relevant gapless spin-2 excitation is the metric graviton, and no preferred order parameter survives, then the physical TT inverse propagator has its massless pole at the Lorentz-invariant root `s=0`.

The quartic observable hierarchy is

```text
S4 preferred spatial order : 6 independent responses
SO3 preferred foliation    : 1 common response
Lorentz metric-only vacuum : 0 shifted massless-pole responses.
```

The same logic extends beyond quartic order: exact ordinary Lorentz invariance protects the vacuum massless cone from a frequency-dependent `k^4+k^6+...` speed law for that same one-particle branch.

### Common photon/graviton cone

If the deconfined U1 phase and massless graviton both couple to one emergent Lorentzian metric, their leading principal cone is the same. `G`, `Z_T` and `Z_A` normalize responses/couplings; they do not tune separate leading speeds.

### Universal gravitational coupling

If the physical history realizes a Lorentz-covariant unitary massless-helicity-2 S-matrix with standard soft factorization, soft-graviton gauge consistency forces a universal gravitational coupling to all species in that S-matrix.

### Compact U1 and alpha

The Hopf topology fixes compactness and the integer charge lattice, not the Maxwell stiffness. A numerical fine-structure constant requires a microscopic calculation of `Z_A`:

\[
\alpha=1/(4\pi Z_A)
\]

in the frozen minimal-charge convention.

---

## D. MICROSCOPIC / PHYSICAL OPEN GATES

### Gravity

1. Complete the optimized full-`H_E` local-block Krylov production after reference-vs-active-cone equivalence certification.
2. Complete the full j=1 representation-RG higher-shell calculation and measure the sign/magnitude of `Delta R_aniso` relative to j=1/2.
3. Construct the full regulated constraint family appropriate to the physical-history habitat, not just a local parent-block constraint.
4. Demonstrate refinement/cylindrical compatibility of physical projectors:
   \[
   \|P_{g+1}I_g-I_gP_g\|\to0.
   \]
5. Freeze physical/semi-classical boundary states and a source-insertion prescription.
6. Compute connected multi-block physical metric correlators and the TT 1PI/pole kernel.
7. Demonstrate the leading massless positive-residue Einstein/Fierz-Pauli pole in that **physical** kernel.
8. Extract the full six-vector blindly and apply the `6 -> 1 -> 0` symmetry hierarchy.
9. If a preferred branch survives, independently demonstrate the corresponding physical order parameter and regulator-rotation independence.

### Light

1. Derive the blocked compact-U1 phase effective action from the same microscopic history/measure.
2. Establish a positive electric and magnetic quadratic sector.
3. Demonstrate deconfinement/Coulomb behavior rather than a gapped/confined phase.
4. Recover two transverse massless poles in the common metric scaling window.
5. Compute `Z_A` without electromagnetic data and only then compare `alpha` blindly.

### Matter

1. Derive an actual microscopic fermionic/spinorial carrier; topology alone only proves compatibility.
2. Recover a Dirac/Weyl principal symbol on the same emergent tetrad.
3. Derive internal gauge groups and representations independently of geometric `SU(2)`.
4. Derive chirality/anomaly cancellation.
5. Only then study flavor/Yukawa/mass operators.

No `137`, fermion masses or generation ratios are to be inferred from unrelated gravitational eigenvalues.

---

## E. EXPERIMENTAL PROGRAMME

Two independent classes must be kept separate.

### Pole/dispersion

The frozen six-direction/polarization extractor tests preferred spatial structure. A completely zero vector is a legitimate and, in the minimal Lorentz metric-only branch, expected result.

### Connected fluctuation/interference

Even with zero vacuum dispersion, a nonzero connected physical metric correlator can create correlated optical phase fluctuations through the already-derived metric-to-phase map.  `R_gamma -> 1` tests isotropy of that covariance, while its absolute cross-spectrum tests the fluctuation amplitude.

Thus a null Lorentz-violation test is not equivalent to a null quantum-geometry test.

---

## F. STRONGEST CURRENT STATEMENT

The candidate construction now has a closed structural geometry chain, a finite physical-projector theorem, a complete unbiased TT observable basis, a conditional common gravity/light IR universality theory, exact compact-U1 kinematics/canonical identities, and a topologically valid unique-spin-structure background for fermions.

What it **does not yet have** is the theory-specific continuum physical history kernel from which the interacting graviton pole, the photon deconfinement/stiffness and a matter spectrum are numerically derived.

Those remaining objects are finite, explicit research targets rather than placeholders for numerology.
