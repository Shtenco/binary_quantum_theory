# Open problems — post integration-certificate

The old version of this file described stages that have since been superseded.
In particular it must no longer be used to reopen the already completed
Peter--Weyl, topology, Euclidean route-HDA or Lorentzian integration work.

## Core architecture

There is no remaining `OPEN` core integration arrow at fixed regulator-safe
Peter--Weyl cutoff.

The final composition theorem gives

\[
\Delta_{\rm full}(\epsilon)
\le
\Delta_{\rm route}(\epsilon)
+C_\times\epsilon+C_{GG}\epsilon^2
\to0
\]

for

\[
G_v=H_{E,v}+(1+\beta^2)H_{L,v},
\]

because the geometry-route \(1/\epsilon\) term cancels exactly, while the
geometry-geometry lapse smear begins at \(O(\epsilon)\). The full HH cutoff is
safe at \(J_{\max}=13/2\) for all-\(j=1/2\) input.

## Genuine research beyond the certificate

The remaining questions are higher-level rather than missing bricks:

- prove a **uniform** bound if \(J_{\max}\to\infty\) jointly with
  \(\epsilon\to0\);
- derive rather than freeze the unique global q=2 face pairing from the bare
  causal rewrite, if uniqueness is demanded;
- select the Lorentzian quantum measure and establish unitarity/reflection or
  causal consistency;
- incorporate matter, chirality and anomaly cancellation;
- set the physical scale/Newton constant from a microscopic observable;
- make blind predictions and obtain independent replication.

A brute-force 11.3M-state Lorentzian HH enumeration is optional regression
work. It is **not** a remaining logical bottleneck for the fixed-cutoff
regulator-limit HDA statement.
