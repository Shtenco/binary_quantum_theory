# Cosmological constant: physical-history saddle protocol

Status: **parameter-identification protocol; no numerical physical Lambda is claimed.**

The completed HDA/ADM calculation constrains the infrared gravitational tensor structure, but the cosmological term cancels from the antisymmetrized constraint bracket.  Therefore HDA closure does not determine a numerical `Lambda`, just as it does not determine the overall Newton normalization.

## 1. Correct microscopic definition

Construct

\[
P_{phys}\to Z[J_g]\to W[J_g]\to\Gamma[g].
\]

The physical vacuum/background is a stationary configuration

\[
\boxed{\delta\Gamma/\delta g_{\mu\nu}|_{g=\bar g}=0.}
\]

Only after this unrestricted physical action exists should one restrict to a homogeneous/isotropic closed-history family to identify a cosmological curvature parameter.

## 2. Einstein/de Sitter positive control

For a closed Lorentzian FRW vacuum with positive cosmological constant,

\[
H^2+1/a^2=\Lambda/3.
\]

At a time-symmetric turning slice `H=0`,

\[
\boxed{\Lambda a_{turn}^2=3.}
\]

The Euclidean continuation is a four-sphere with

\[
R=4\Lambda=12/r^2,
\qquad
\boxed{\Lambda r^2=3.}
\]

This is why a unit-radius regular `S4` Regge positive control can return a dimensionless number near three.  It validates the curvature estimator; it is **not** a prediction that the observed cosmological constant equals the finite regulator curvature.

## 3. Dimensionless zero-fit target

Let the physical history define one coarse length unit `a_*`.  Freeze first

\[
\boxed{\hat\Lambda=\Lambda a_*^2}
\]

entirely in internal units.  No absolute metre/Planck calibration is required at this stage.

Only after the common scale is independently derived or fixed by the declared one-datum rule may `Lambda` be converted to physical inverse-length squared units.

## 4. Lambda is not vacuum energy density

In Einstein normalization,

\[
\rho_\Lambda=\Lambda/(8\pi G).
\]

Therefore predicting vacuum energy density requires both `Lambda` and `G`.  The project must keep distinct

```text
finite Regge curvature diagnostic;
physical 1PI background Lambda;
vacuum energy density rho_Lambda.
```

No finite Peter-Weyl eigenvalue or local tetrahedral splitting is a dark-energy prediction without a derived map.

## 5. Quantum-corrected backgrounds

If the physical action contains important higher-curvature or nonlocal terms, the saddle need not satisfy the Einstein de Sitter relation exactly.  Then report the actual background invariants and define `Lambda_eff` only through a declared low-curvature matching procedure.

A number must not be forced into the Einstein symbol `Lambda` if the physical saddle fails the Einstein equation beyond the measured truncation error.

## 6. Falsification gates

Before claiming a cosmological prediction:

1. physical-projector/refinement tests pass;
2. the background saddle is stable under regulator/refinement changes;
3. homogeneous/isotropic reduction is stable against unrestricted perturbations;
4. the tensor sector around the saddle has a positive physical residue;
5. `hat Lambda` is stable under the declared derivative truncation;
6. no cosmological datum is used to select the microscopic branch before freeze.

## 7. Canonical arrow

\[
\boxed{
\text{physical history}\to\Gamma[g]\to\bar g_{vac}\to\Lambda_{eff}.
}
\]

Not `HDA -> Lambda`, and not `finite curvature eigenvalue -> observed dark energy`.
