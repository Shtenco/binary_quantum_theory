# K5 quantum geometry bridge

Status: **exact finite SU(2) quantum-link calculation; strongest current upstream geometrogenesis control, but not yet the Lorentzian Hamiltonian constraint or a continuum theory**.

## 1. Finite laboratory

Take the dual graph `K5` of the boundary of a 4-simplex.  Its five four-valent nodes represent five tetrahedral cells and its ten links represent shared triangular faces.

Each link carries the five-state `SO(5)` vector quantum-link representation

\[
\mathbf 5=(\mathbf2,\mathbf2)\oplus(\mathbf1,\mathbf1),
\]

with an active geometric bi-doublet plus a gauge-singlet off/defect state.

The raw link Hilbert space has

\[
5^{10}=9\,765\,625
\]

states.  Exact Gauss reduction leaves only

\[
\boxed{\dim\mathcal H_G(K_5)=140}.
\]

The fully active five-tetrahedron sector has

\[
\boxed{\dim\mathcal H_{full}=2^5=32},
\]

one spin-1/2 intertwiner qubit at each tetrahedral node.

This makes the complete closed finite geometry exactly diagonalizable/projectable without a continuum ansatz.

## 2. Exact Wilson-loop operators

For every one of the ten triangles of `K5`, construct the non-Abelian Wilson operator

\[
W_\triangle=\operatorname{Tr}(U_{e_1}U_{e_2}U_{e_3}).
\]

After exact Gauss projection each is a sparse `140 x 140` Hermitian matrix.  The combined operator is

\[
W=\sum_{\triangle\subset K_5}W_\triangle.
\]

The vacuum is the state with all ten links in their singlet/off sector.

Combinatorially, four triangle flips are the minimum required to transform the empty active graph into the fully active `K5` graph.  Therefore the first full-geometry component occurs in

\[
P_{full}W^4|0\rangle.
\]

## 3. Independent four-simplex target

Independently contract five four-valent spin-1/2 SU(2) intertwiners along the ten links of `K5` with the invariant epsilon tensor.  This gives the 32-component five-tetrahedron vertex state

\[
|V_5\rangle.
\]

The construction is independent of the quantum-link Wilson history: it is the direct spin-network evaluation of one 4-simplex boundary.

## 4. No-space -> 4-simplex overlap

Normalize the fully active component of the fourth Wilson power,

\[
|\Psi_4\rangle=\frac{P_{full}W^4|0\rangle}{\|P_{full}W^4|0\rangle\|}.
\]

Then

\[
\boxed{
|\langle \widehat V_5|\Psi_4\rangle|^2
=\frac{90}{91}
=0.989010989\ldots
}
\]

or amplitude overlap

\[
\boxed{0.9944903162}.
\]

Thus four closed quantum-link curvature events generate a state with `98.9011%` fidelity to the independently constructed 4-simplex boundary state without fitted history weights.

The full-geometry component is still only about `1.2524%` of the norm of `W^4|0>`; the statement above is the conditional wavefunction inside the fully active sector, not a claim that pure Wilson dynamics strongly prefers that sector.

On the twelve basis components where the direct four-simplex tensor is nonzero, the conditional fidelity is even

\[
\boxed{\frac{300}{301}}.
\]

## 5. The missing 1/91 is structural

The mismatch is not repaired by reweighting the ten minimal triangle sets or their `240` ordered four-loop histories.

Nor is it removed by longer powers of the symmetric Wilson operator.  The full-sector Krylov space generated by

\[
P_{full}W^n|0\rangle,
\qquad n=4,6,\ldots,30,
\]

grows in rank, but the target component orthogonal to it remains

\[
\boxed{
\epsilon_{Wilson}^2=\frac1{91}.
}
\]

Therefore the missing amplitude is not a bad choice of four-loop weights.  It lies outside the tested pure-Wilson/Yang--Mills-like cyclic sector.

## 6. Scalar volume is not enough

Define the nondegenerate-node counting/volume proxy

\[
V=\sum_v P_{k_v=4}.
\]

At microscopic spin `j=1/2`, absolute volume is proportional to the identity inside each fixed `k=4` intertwiner qubit.  Accordingly,

\[
\mathcal K(W,V)|0\rangle
\]

has the same seven-dimensional cyclic sector as pure `W` in this finite model, and the target defect remains exactly

\[
\boxed{1/91}.
\]

This directly falsifies the idea that `curvature + scalar volume potential` is sufficient to reproduce the simplicial gravity channel.

## 7. One tensorial shape observable closes the channel

At a four-valent `k=4` node, the exact spin-1/2 intertwiner basis obeys for example

\[
J_1\cdot J_2=-\frac14I-\frac12 Z_L.
\]

Define the gauge-invariant shape operator

\[
Z_{shape}=\sum_{v:k_v=4}Z_L^{(v)}.
\]

Then the cyclic space generated by `{W,Z_shape}` from the vacuum has dimension `31`, but it contains the complete target four-simplex state exactly to machine precision:

\[
\boxed{
\|V_5-P_{\mathcal K(W,Z)}V_5\|^2<5\times10^{-29}.
}
\]

Using the other exact tetrahedral logical operators gives the same qualitative conclusion:

- `{W,Y_shape}` reaches the target with residual below `10^-27`;
- `{W,X_shape}` generates the full `140`-dimensional Gauss Hilbert space.

Thus the missing channel is specifically **intertwiner/shape information**, not scalar occupancy or volume.

## 8. Shortest operator-depth certificate

Use algebraic words in `W` and `Z_shape` acting on the vacuum.  The target projection behaves as

| maximum word depth | best target defect |
|--:|--:|
| 0 | 1 |
| 1 | 1 |
| 2 | 1 |
| 3 | 1 |
| 4 | `1/91` |
| 5 | `1/91` |
| 6 | `0.0055915038647` |
| 7 | `<5e-29` |

Therefore

\[
\boxed{d_{word}^{min}=7}.
\]

Inside depth `<=7`, allowing at most one shape insertion is still insufficient.  Allowing at most two is sufficient.  Exhaustive subset search also finds that at least seven projected word-states are needed for an exact representation in that shortest-depth, at-most-two-shape sector.

One exact seven-word spanning set is

\[
\boxed{
\{W^4,\;W^4Z,\;W^3ZW,\;W^4Z^2,\;W^3ZWZ,\;W^3Z^2W,\;W^4ZW^2\}.
}
\]

This is an algebraic spanning statement, not seven physical time steps and not yet a derivation of continuum `EEF`.  Its importance is structural: in the shortest exact completion, at least **two** tensorial geometry insertions are needed in addition to curvature operations.

## 9. Relation to the canonical gravity structure

The finite result sharply separates three operator classes:

\[
\boxed{
F\text{-like Wilson curvature alone}\quad\text{fails by }1/91,
}
\]

\[
\boxed{
F+V_{scalar}\quad\text{still fails by }1/91,
}
\]

but

\[
\boxed{
F+\text{tensorial flux/shape}\quad\text{contains the full 4-simplex channel}.
}
\]

This is qualitatively what canonical gravity requires: the leading gravitational scalar is tensorial in two fluxes and one curvature, schematically `EEF`, rather than a pure Wilson `F^2` loop gas or a curvature-plus-volume potential.

A first direct grasped operator `Herm[epsilon J J F]` was also tested.  Although gauge-invariant and nontrivial, its fully symmetric sum from the vacuum remains in the same `1/91`-deficient cyclic sector.  This negative result shows that the correct ordering/volume-commutator/orientation structure cannot be omitted merely because an operator looks like `EEF` symbolically.

The next operator test must therefore use the actual Thiemann-type finite form with holonomy and volume commutators rather than an arbitrary grasping.

## 10. Reproduction

```bash
python scripts/k5_quantum_geometry_bridge.py \
  --output verification_results/k5_quantum_geometry_bridge.json
```

## 11. Boundary of the result

This calculation establishes an exact finite microscopic bridge between

\[
\text{off-link vacuum}
\to
\text{dynamic SU(2) quantum-link geometry}
\to
\text{five-tetrahedron 4-simplex boundary channel}.
\]

It does **not** yet establish:

1. the correct real Lorentzian Hamiltonian constraint;
2. hypersurface-deformation algebra closure;
3. a `z=1`, three-spatial-dimensional continuum phase;
4. universality under link representation/operator-ordering changes;
5. matter/chirality/anomaly cancellation;
6. an experimental prediction.

The main new conclusion is narrower and stronger: pure Wilson/Yang--Mills-like geometrogenesis is mathematically insufficient in this exact finite laboratory, while adding genuine tensorial quantum geometry removes the obstruction completely.
