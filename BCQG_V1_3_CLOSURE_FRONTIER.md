# BCQG v1.3 — compressed closure frontier

Status: **canonical research frontier after the 2026-09-04 audit.**

This note does not claim experimental confirmation.  Its purpose is to separate
independent physics obligations from redundant GR cross-checks.

## 1. What is already closed at the declared microscopic / first-block level

The following parts are no longer the principal uncertainty:

- the chosen `q=2 -> recursive PL S3` carrier and its finite topology checks;
- physical Euclidean sine operator `H_E=(T-T^dagger)/(2i)`;
- operator-first positive route symbol and kernel-safe square-root construction;
- Hermitian Lorentzian completion `S=-i(L_raw-L_raw^dagger)/2` once `L_raw` is fixed;
- fixed finite-depth representation wall on the declared microscopic habitat;
- microscopic operator-first HDA composition theorem;
- exact first collective Euclidean Krylov enlargement;
- emergence of a six-dimensional intrinsic coarse metric carrier;
- exact coarse flux-to-metric calibration with
  `s_min=1/sqrt(6)`, `s_max=1/sqrt(3)`, `cond=sqrt(2)`;
- direct homogeneous gravitational block theorem `P G P = 0`, forcing the
  nontrivial gravitational kinetic tensor to arise from depth-two return;
- exact Maxwell/U(1) kinematic coupling through the metric-dependent Hodge star;
- five-channel balanced photon interferometric sensitivity to the traceless
  metric sector, with the uniform trace direction as the common-mode null.

The historical fixed-`q_123` continuation on charged intermediate states is
superseded.  The v1.3 Lorentzian calculation uses the tetrahedrally covariant
four-leg volume completion.

## 2. The old GR killer contains redundant targets

The historical numerical AND gate asks simultaneously for

`D_space -> 3`, `c_DW -> 1/2`, `z -> 1`, constraint ranks `(3,3,1,0)`,
`r_SC -> 0`, `N_phys -> 2`, and collective `Delta_HH -> 0`.

They remain valuable blind cross-checks, but under the explicit IR hypotheses
of `BCQG_IR_UNIVERSALITY_CLOSURE_THEOREM.md` they are not independent miracles.

If the same refinement family satisfies:

1. a uniformly nondegenerate 3D collective metric;
2. a local two-derivative leading IR scalar constraint;
3. the standard first-class hypersurface-deformation algebra and stable ranks
   `(3,3,1,0)`;
4. no surviving second-class sector;

then the repository's exact ADM/HDA identity gives

`c_DW = 1/2` and `A B = 1`,

hence leading `z=1`, while Dirac counting gives exactly two physical
configuration modes.  Uniform bilipschitz regularity on recursive PL-S3 fixes
local spatial dimension to three.

Therefore the irreducible internal GR burden is H1--H4, not four separately
fitted target numbers.

## 3. Canonical depth-two dynamics has no free denominator

On the homogeneous six-edge metric carrier the direct gravitational block is
zero:

`P C P = 0`.

Writing the coupled constraint on `P + Q` as

    C = [[0, B], [B^dagger, D]],

with `D = Q C Q`, the exact zero-constraint Schur/Feshbach reduction is

    C_eff(0) = - B D^{-1} B^dagger
             = - P C Q (Q C Q)^{-1} Q C P.

No phenomenological denominator may be chosen to improve agreement with GR.
If `Q C Q` has an exact zero mode that couples to `P`, that mode belongs in an
enlarged low-energy carrier.  Only the orthogonal gapped range may be inverted.

This is the canonical target-independent definition of the collective depth-two
effective scalar.

## 4. Lorentzian v1.3 execution frontier

The earlier 24+24 direct V2 workflow was cancelled before a final science
result.  The pairing stabilizer theorem gives three free ordered-term orbits of
size eight.  However orbit reduction is allowed only after exact Peter-Weyl
transport and direct production-word covariance are independently validated.

The corrected transport gate no longer requires the false one-dimensional
pseudoscalar character for the summed Euclidean column.  It checks the actual
unitary/group transport representation `U_h`.

The V3 workflow is preregistered to use:

- 12 direct heavy V2 terms;
- six representative/held-out production-word covariance pairs, one per orbit
  and mode;
- only after all six pass, exact materialization of the other 36 terms by `U_h`;
- the existing full 48-index Hermitian collector for the final `S_0` column.

A failed held-out pair forbids reconstruction.  No threshold, sign, cutoff or
operator coefficient may be changed after inspecting the V3 science output.

## 5. Four genuinely independent internal closure obligations

### H1 — metric regularity under refinement

Extend the measured coarse flux-to-metric map through the same dynamic
refinement family and establish level-independent positive lower and finite
upper singular-value bounds.  This is stronger than tuning a heat-kernel window.

### H2 — IR locality / relevance

Show that the Schur-reduced collective scalar has a local two-derivative ADM
leading term, while non-ADM operators are gapped, higher-derivative or otherwise
irrelevant in the same refinement limit.

### H3 — collective first-class algebra and complete ranks

Using the frozen S3 harmonic lapse family and measured refinement variable
`epsilon=h/R`, show direct convergence of `[H[N],H[M]]` to the diffeomorphism
target and stabilize independent ranks to `(3,3,1,0)`.

### H4 — no second-class remainder

Show `r_SC=0` after the same collective reduction, including reducibility and
null-direction checks.

If H1--H4 hold on one common refinement family, then within the declared local
two-derivative universality class the IR gravitational theory is ADM/GR up to
the remaining Newton/cosmological normalization and irrelevant corrections.

## 6. Physics that internal closure cannot replace

Even perfect H1--H4 do not prove the theory is nature.  Still independent are:

- absolute `G_N` and `Lambda` matching from microscopic observables;
- a Lorentzian quantum measure / unitarity or equivalent causal consistency;
- complete matter content, chirality and anomaly cancellation beyond the
  already established Maxwell/U(1) kinematic bridge;
- blind experimental tests, including any photon-interference prediction only
  after its physical scale and geometry correlator are derived.

## 7. Current honest status

The microscopic architecture is a serious computable candidate and the
collective metric bridge is substantially stronger than the earlier static
rank-one picture.  The mathematical path to GR is now sharply compressed, but
**collective H1--H4 are not yet all directly established on one refinement
family** and the corrected v1.3 Lorentzian finite column is still an active
falsifier.

Consequently the correct claim is:

> BCQG has a closed microscopic candidate architecture, a direct six-component
> collective metric carrier, a conditional IR universality theorem to ADM/GR,
> and explicit remaining falsifiers.  It is not a 100% experimentally proven
> theory of quantum gravity.
