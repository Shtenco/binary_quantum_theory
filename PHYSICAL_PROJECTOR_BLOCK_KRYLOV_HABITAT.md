# Physical projector on a constraint-generated block-Krylov habitat

Status: **production architecture sharpened by the certified 32D logical-master no-go; no physical zero mode is claimed here**.

The retained Euclidean `32 x 32` logical Gram is full rank. Therefore a physical zero sector, if it exists, cannot be represented by a state confined to the bare all-`j=1/2` logical carrier. The correct next object is a physical projector acting on an enlarged graph/representation habitat while the q=2 logical states remain boundary/source vectors.

---

## 1. Boundary block

Let

\[
V_0=(|\psi_1\rangle,\ldots,|\psi_{32}\rangle)
\]

be the complete canonical all-`j=1/2` logical boundary block.

The desired first physical quantity is not

\[
\ker(P_{log}\mathbb M P_{log}),
\]

but the full-space boundary projection

\[
\boxed{
G_{phys}^{(0)}
=V_0^\dagger P_{phys}V_0.
}
\]

A nonzero `G_phys^(0)` is possible even when the restricted logical master is positive definite, because a full physical zero mode may have support outside `H_log` while retaining nonzero overlap with the boundary block.

---

## 2. Exact elementary example

Take

\[
M=\begin{pmatrix}1&-1\\-1&1\end{pmatrix}\ge0.
\]

The full kernel is spanned by

\[
|0\rangle=\frac1{\sqrt2}(1,1)^T.
\]

Choose boundary carrier `H_b=span(e1)`. Then

\[
e_1^TMe_1=1>0,
\]

so the restricted one-dimensional master has no zero vector. Nevertheless

\[
P_0=|0\rangle\langle0|
\]

and

\[
\boxed{\langle e_1|P_0|e_1\rangle=1/2.}
\]

Thus

```text
restricted boundary kernel empty
```

is fully compatible with

```text
boundary overlap with an enlarged physical zero mode nonzero.
```

This is exactly the distinction required after `EUCLIDEAN_LOGICAL_MASTER_BASELINE_RESULT.md`.

---

## 3. Constraint-generated, parameter-independent habitat

There are five node locations. Before the relative Lorentzian coefficient is physically frozen, keep the Euclidean and Lorentzian operators as separate generators,

\[
\mathcal O=
\{E_0,\ldots,E_4,L_0,\ldots,L_4\},
\]

where

\[
E_v=H_{E,v}^{sine},
\qquad
L_v=H_{L,v}.
\]

Define the block-Krylov sequence

\[
\mathcal K_0=\operatorname{span}V_0,
\]

\[
\mathcal K_d
=\operatorname{span}
\{O_{a_d}\cdots O_{a_1}V_0:\ 0\le r\le d,\ O_a\in\mathcal O\}.
\]

Before linear dependencies, the generated-vector counts obey

\[
\dim\mathcal K_0\le32,
\]

\[
\boxed{\dim\mathcal K_1\le32(1+10)=352},
\]

\[
\boxed{\dim\mathcal K_2\le32(1+10+10^2)=3552}.
\]

These are **numbers of generated vectors**, not dimensions of the ambient Peter--Weyl basis supporting each vector. Rank-revealing orthogonalization must determine the actual Krylov dimensions.

This is dramatically smaller than treating every representation-admissible spin network in the reachable support as an independent dense variable.

---

## 4. Relation to the existing reachable-space upper bounds

The repository's full Lorentzian support enumeration gives, before amplitude cancellations:

### after one complete `H_E+H_L`

\[
1843\ \text{Gauss-admissible spin assignments},
\]

with total spin-network dimension

\[
\boxed{9750},
\]

and largest final spin `j=5/2`.

### after two complete local Hamiltonians

\[
615884\ \text{Gauss-admissible spin assignments},
\]

with total final spin-network dimension

\[
\boxed{11314085},
\]

and largest final spin `j=11/2`, while the conservative transient wall remains `Jmax=13/2`.

These large numbers characterize the **ambient support union**. A block-Krylov calculation stores only the actual operator-generated vectors required from the 32-dimensional boundary block, with local recoupling matrices cached and ambient sparse states streamed.

---

## 5. Why split `E` and `L` before fixing beta

For a real relative coefficient

\[
G_v(\lambda)=E_v+\lambda L_v,
\]

one should not choose `lambda` merely to make a finite result attractive. On a parity-complete enlarged state, Euclidean and Lorentzian pieces can contribute to the same output-parity equation through different input-parity components, so their mixed term is physically relevant.

The master is a quadratic pencil

\[
\boxed{
\mathbb M(\lambda)
=M_0+\lambda M_1+\lambda^2 M_2.
}
\]

The parameter-independent Krylov habitat generated by the separate `E_v,L_v` operators supports three distinct questions:

1. Is there a common zero sector stable for an interval of `lambda`?
2. Are zeros present only at isolated tuned values of `lambda`?
3. Does the finite spectrum approach `lambda` independence under the same refinement/continuum limit that gives the classical beta-cancellation theorem?

Until one of these is resolved, a tuned finite `lambda` is not a physical prediction.

---

## 6. Projector through the master heat kernel

For a positive finite master constraint,

\[
P_0=\mathbf1_{\{0\}}(\mathbb M)
=\lim_{\tau\to\infty}e^{-\tau\mathbb M}.
\]

Therefore the boundary physical Gram is

\[
\boxed{
G_{phys}^{(0)}
=\lim_{\tau\to\infty}
V_0^\dagger e^{-\tau\mathbb M}V_0.
}
\]

This is a natural block-Lanczos target: start from `V0`, orthonormalize its Gram, generate successive master-Krylov blocks, build the small block-tridiagonal representation, and evaluate the projected matrix function there.

The repository already verifies the required finite Feshbach/block-Krylov identities on nonorthonormal coarse blocks. Those identities provide algebraic infrastructure only; the BQG master moments remain to be computed from the Peter--Weyl constraint operators.

---

## 7. Moment formulation

For an initial nonorthonormal boundary block `V`, define

\[
K=V^\dagger V,
\qquad
A=V^\dagger\mathbb M V,
\qquad
B=(\mathbb M V)^\dagger(\mathbb M V).
\]

After whitening by `K^{-1/2}`, the first block-Lanczos residual Gram is

\[
\boxed{
B_1^\dagger B_1
=K^{-1/2}
\left[B-A^\dagger K^{-1}A\right]
K^{-1/2}.
}
\]

Higher moments extend the block Jacobi matrix. This avoids building a dense matrix on the entire ambient 11-million-dimensional support.

---

## 8. Required production steps

The shortest physical-projector programme is now:

```text
A. finish full epsilon-oriented L_v amplitudes
B. freeze the Hermitian physical constraint convention
C. implement E_v/L_v actions on generic sparse Krylov vectors
D. generate parameter-independent K1 and K2 from all 32 boundary columns
E. rank-reveal/orthonormalize the generated block vectors
F. build master pencil moments M0,M1,M2
G. test zero-sector stability versus lambda without fitting observations
H. build block-Lanczos representation of M(lambda)
I. compute V0^dag exp(-tau M) V0 for increasing tau and Krylov depth
J. separate genuine zero convergence from small positive finite-regulator eigenvalues
K. only after a controlled projector exists, insert metric/volume relational sources
```

---

## 9. Scalar/cosmology consequence

The legal scalar source amplitude becomes

\[
Z_{phys}[J]
=\langle\psi_{out}|
P_{phys}\,\mathcal T e^{J\cdot O_{scalar}}
|\psi_{in}\rangle,
\]

where `P_phys` acts in the enlarged constraint habitat even though `psi_in/out` may remain q=2 logical boundary states.

Connected scalar Hessians then come from

\[
W=\log Z_{phys}.
\]

This is the first route that simultaneously respects:

- the full-rank logical Euclidean master no-go;
- graph/representation dressing;
- parity mixing;
- the master-constraint theorem;
- the prohibition `constraint resolvent z != physical omega`.

---

## Claim boundary

Block-Krylov compression is an exact linear-algebra strategy at finite depth when all generated vectors and inner products are exact in the declared cutoff. It does not prove that a finite physical zero mode exists, that the Krylov sequence converges uniformly with regulator removal, or that the resulting scalar response explains dark matter or dark energy.