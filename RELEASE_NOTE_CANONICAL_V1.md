# Canonical package v1 release note

This release consolidates the already-derived gravity calculations into one canonical package and changes status semantics so that stronger extensions and external experiments no longer masquerade as unfinished core theory tasks.

The authoritative verification is `.github/workflows/core-regression.yml`.
