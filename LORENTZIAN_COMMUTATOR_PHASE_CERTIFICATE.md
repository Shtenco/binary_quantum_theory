# Lorentzian commutator-phase certificate

Status: **conditional analytic certificate inside the declared Thiemann nested-bracket quantization**.

This note resolves the complex **phase** ambiguity of the frozen raw Lorentzian one-body block. It does not yet determine the remaining real normalization or prove uniqueness of the full factor ordering.

---

## 1. Inputs already fixed by the repository

The physical Euclidean operator used in the expensive Lorentzian calculation is the sine-Hermitian operator

\[
H_E^{sine}=\frac{T-T^\dagger}{2i}.
\]

The raw extrinsic-curvature building block is stored as

\[
K_{raw}=[V,H_E^{sine}],
\]

and the covariant legs are implemented as

\[
C_{raw}(O)=h[h^{-1},O].
\]

The raw Lorentzian structural triple is

\[
L_{raw}
\sim
\operatorname{Tr}
\left[
C_{raw}(K_{raw})
C_{raw}(K_{raw})
C_{raw}(V)
\right].
\]

The exact environment-trace result gives

\[
L_{raw,1body}
=i\,1.3389293521464034\,Y+O(10^{-16}).
\]

---

## 2. Classical nested-bracket structure

The declared Lorentzian architecture follows the standard real-connection Thiemann rewriting

\[
T[N]
\propto
\int N\,\epsilon^{abc}
\operatorname{Tr}
\bigl(
\{A_a,K\}
\{A_b,K\}
\{A_c,V\}
\bigr),
\]

with integrated extrinsic curvature generated by

\[
K\propto\{V,H_E[1]\}.
\]

Thus, after substituting the two `K` factors, the structural expression contains exactly five Poisson brackets:

```text
2 inner brackets:  {V,H_E} inside the two K legs
2 outer brackets:  {A,K}
1 volume bracket:  {A,V}
--------------------------------
total = 5.
```

This bracket architecture is the one underlying the repository's `K=[V,H_E]`, `C(K)` and `C(V)` operators.

Primary references for the construction are Thiemann's QSD programme (`arXiv:gr-qc/9606089`) and the explicit Hamiltonian rewrite used by Borissov, De Pietri and Rovelli (`arXiv:gr-qc/9703090`). The holonomy-volume realization of the Euclidean bracket structure is also reviewed and recalculated in Yang and Ma (`arXiv:1505.00225`).

---

## 3. Quantization phase

Under the standard canonical correspondence

\[
\{\ ,\ \}
\longrightarrow
\frac{1}{i\hbar}[\ ,\ ],
\]

five nested brackets contribute

\[
\left(\frac1{i\hbar}\right)^5
=
\frac{-i}{\hbar^5}.
\]

All omitted powers of `kappa`, `beta`, regulator lengths and the remaining `hbar` factors are real for the declared real-connection theory. Therefore the universal complex phase multiplying the repository's raw structural triple is

\[
\boxed{-i}.
\]

An overall **real sign** can still depend on the exact classical sign convention absorbed into the definition of `H_L`; it does not change the Hermiticity conclusion.

---

## 4. Consequence for the frozen one-body block

Because

\[
L_{raw,1body}
=i c_Y Y,
\qquad
c_Y=1.3389293521464034,
\]

we obtain

\[
-iL_{raw,1body}
=c_Y Y+O(10^{-16}).
\]

Thus the canonical commutator phase selects the anti-Hermitian-to-Hermitian branch

\[
\boxed{
H_{phase}
\propto
\frac{L_{raw}-L_{raw}^\dagger}{2i},
}
\]

not the naive even projection `(L_raw+L_raw^dagger)/2` applied before restoring the missing bracket phase.

The phase-completed block is Hermitian and has dimensionless structural eigenvalues

\[
\boxed{\lambda=\pm1.3389293521464034}
\]

before the remaining real normalization.

---

## 5. What this closes

Within the **declared nested-bracket quantization convention**, the question

```text
which universal complex phase must multiply the raw K-K-V triple?
```

is answered:

```text
-i, because the substituted Lorentzian term contains five Poisson brackets.
```

Therefore the nonzero raw `iY` amplitude is not removed merely by requiring a Hermitian physical operator: after restoring the canonical bracket phase it becomes a Hermitian `Y` block.

Executable audit:

```bash
python scripts/lorentzian_commutator_phase_gate.py \
  --output verification_results/LORENTZIAN_COMMUTATOR_PHASE.json
```

---

## 6. What remains genuinely open

This result does **not** yet fix:

1. the complete real `kappa/beta/hbar` normalization in the repository's dimensionless units;
2. the overall real sign convention connecting `H_L` to the classical Lorentzian correction;
3. uniqueness against every alternative symmetric factor ordering of the full noncommuting node operator;
4. the two-node `H_E+H_L+R_Q` commutator and its off-shell HDA residual;
5. the collective-spin/refinement stability of the one-body `Y` coefficient.

The next operator bottleneck is therefore narrower:

```text
real normalization/sign + full factor-ordering freeze
 -> completed H_L
 -> two-node Lorentzian route-coupled HDA.
```

The present certificate must not be interpreted as a physical mirror mass until those steps and scale setting are complete.
