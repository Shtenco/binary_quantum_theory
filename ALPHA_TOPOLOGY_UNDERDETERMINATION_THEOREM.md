# Fine-structure constant: topology fixes charge quantization, not the Maxwell stiffness

Status: **exact underdetermination statement.**

The q=2 Hopf bundle supplies a compact `U(1)` connection with first Chern number one.  This fixes the topological normalization of phase winding/charge quantization.  It does **not** determine the coefficient of the Maxwell kinetic term.

---

## 1. Compactness fixes a charge lattice

The link phase has period

\[
\theta\sim\theta+2\pi.
\]

The Hopf curvature obeys

\[
\frac1{2\pi}\int_{S^2}F=1.
\]

Hence Wilson representations are labelled by integer charge in the minimal topological normalization.

This normalization prevents an arbitrary continuous rescaling of the compact connection without simultaneously changing its charge lattice.

---

## 2. A continuous stiffness remains

For every positive real number `Z_A`,

\[
\boxed{
\Gamma_A[Z_A]
=-\frac{Z_A}{4}\int d^4x\sqrt{-g}\,F_{\mu\nu}F^{\mu\nu}
}
\]

has

- the same local `U(1)` gauge symmetry;
- the same compact phase period;
- the same first Chern class;
- the same integer Wilson-charge labels;
- the same leading Maxwell characteristic cone.

Therefore topology and gauge symmetry alone leave a continuous one-parameter family of physically inequivalent kinetic normalizations.

---

## 3. Canonical normalization exposes the coupling

Set

\[
A_c=\sqrt{Z_A}\,A.
\]

A minimally charged compact matter phase couples originally as

\[
D=\partial+iA.
\]

In canonical variables,

\[
D=\partial+i\frac1{\sqrt{Z_A}}A_c.
\]

Thus

\[
\boxed{e=Z_A^{-1/2}}
\]

and in the declared rationalized convention

\[
\boxed{\alpha=\frac1{4\pi Z_A}.}
\]

The continuous stiffness is therefore the physical coupling once the topological charge unit is fixed.

---

## 4. Underdetermination theorem

Because every `Z_A>0` is compatible with the closed kinematic/topological statements,

\[
\boxed{
\text{Hopf topology}
+\text{compact }U(1)
+\text{gauge covariance}
+\text{Maxwell tensor form}
\not\Rightarrow
\text{a numerical }\alpha.
}
\]

A number such as `alpha^{-1} ~ 137` cannot be inferred from those structures without additional microscopic dynamics.

---

## 5. What would constitute a genuine derivation

A valid zero-fit calculation must determine the coefficient of the physical blocked phase effective action,

\[
\Gamma_{phase}^{(2)}
\longrightarrow
-\frac{Z_A^{IR}}4\int F^2,
\]

from the q=2 microscopic history/measure, with

- deconfinement demonstrated;
- regulator/refinement stability demonstrated;
- charge normalization frozen by the Hopf bundle before the numerical result is opened;
- matter/vacuum polarization included if the claimed observable scale requires it;
- no experimental electromagnetic constant used in choosing the branch or fitting `Z_A`.

Only then may

\[
\alpha_{pred}=1/(4\pi Z_A^{IR})
\]

be compared blindly with experiment.

---

## 6. Explicit anti-numerology rule

Peter-Weyl eigenvalues, tetrahedral `E/T2` splittings, Regge residues and gravitational HDA coefficients live in different operators/normalizations.  They are not candidates for `alpha` unless a derived operator map proves that they are the Maxwell stiffness.

Therefore the repository forbids post-hoc searches for `137` or particle masses in unrelated spectra.
